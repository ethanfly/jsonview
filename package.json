{
    "name": "jsonview",
    "version": "1.0.1",
    "private": true,
    "scripts": {
        "dev": "vite",
        "ensure-nsis": "node -e \"if (process.platform === 'win32') { console.log('[ensure-nsis] 未找到 scripts/ensure-nsis-windows.ps1，跳过 NSIS 预检查'); }\"",
        "build": "npm run ensure-nsis && vite build && tauri build",
        "build:web": "vite build",
        "tauri": "tauri",
        "tauri:dev": "tauri dev",
        "tauri:build": "tauri build",
        "preview": "vite preview"
    },
    "dependencies": {
        "@tauri-apps/plugin-dialog": "^2.6.0",
        "@tauri-apps/plugin-global-shortcut": "^2.3.1",
        "pinia": "^2.1.7",
        "vue": "^3.5.0"
    },
    "devDependencies": {
        "@tauri-apps/api": "^2.0.0",
        "@tauri-apps/cli": "^2.0.0",
        "@vitejs/plugin-vue": "^5.0.0",
        "typescript": "^5.6.0",
        "vite": "^5.0.0"
    }
}